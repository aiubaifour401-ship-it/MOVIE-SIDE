apiVersion: v1
kind: Service
metadata:
  name: cineverse-backend-service
  namespace: cineverse-production
  labels:
    app: cineverse-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: cineverse-backend
