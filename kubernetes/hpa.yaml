apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: cineverse-backend-hpa
  namespace: cineverse-production
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: cineverse-backend-deployment
  minReplicas: 3
  maxReplicas: 50
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 75
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
